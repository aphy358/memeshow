
var formatDuaration = (function(num) {
  return num / 1000 << 1 >> 1
});

var getTimeDiff = t => {
  return Date.now() - t
}

var accurate2 = num => num.toFixed(2)

module.exports = ({
  formatDuaration,
  getTimeDiff,
  accurate2,
});