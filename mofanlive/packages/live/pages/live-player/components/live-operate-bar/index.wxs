
const getStarCountText = num => {
  if (num >= 100000) {
    num = parseFloat((num / 10000).toFixed(1)) + '万'
  } else if (num >= 10000) {
    num = parseFloat((num / 10000).toFixed(2)) + '万'
  }
  return num
}

module.exports = {
  getStarCountText,
}