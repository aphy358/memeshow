<!-- Switch Component -->

<switch
    checked="{{ checked }}"
    disabled="{{ disabled }}"
    color="{{ disabled ? '#febfcb' : '#fe2c54'}}"
    type="{{ type }}"
    bindchange="onChange"
    class="mf-switch__{{ type }}"
>
</switch>
