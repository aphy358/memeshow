<!-- List Item Video Component -->

<view class="mf_list-item-video">
    <video
        src="{{ url }}"
        autoplay="{{ autoplay }}"
        muted="{{ muted }}"
        controls="{{ controls }}"
    >
    </video>
</view>